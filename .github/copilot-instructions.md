# Copilot instructions — Willow Childcare OS

This repo is a childcare / kindergarten operations app in `app/`.

When answering:
- Treat `app/src` as the source of truth.
- Demo accounts: director@willow.care, teacher@willow.care, parent@willow.care (password `demo`).
- Do not revive the old global “top 15 most-forked GitHub repos” clone list (TensorFlow, Bootstrap, etc.).
